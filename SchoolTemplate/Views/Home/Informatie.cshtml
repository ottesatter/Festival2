@using SchoolTemplate.Database;
@model List<Festival>

@foreach (Festival f in Model)
{
    <p>
        naam: @f.naam <br />
        plaats: @f.plaats <br />
        beschrijving: @f.beschrijving 
    </p>
}